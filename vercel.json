{
  "version": 2,
  "installCommand": "uv pip install --system -r requirements.txt --break-system-packages || pip install -r requirements.txt --break-system-packages",
  "buildCommand": "mkdocs build",   // 去掉 --strict
  "outputDirectory": "site"
}